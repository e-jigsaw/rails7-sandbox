<div data-controller="hello"></div>

<h1 class="mt-20 bg-red-500"><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to "Edit", edit_article_path(@article) %>
<%= button_to "Destroy", article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %>
